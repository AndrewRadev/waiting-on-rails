#! /usr/bin/env ruby

filename = File.expand_path(File.dirname(__FILE__) + '/../../../tmp/rails_output')

File.open(filename, 'r') do |pipe|
  begin
    while line = pipe.readline
      puts line
    end
  rescue EOFError
  end
end
